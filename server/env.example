DATABASE_URL=postgresql://jim:jim@localhost:5433/jim?schema=public

# JWT secrets (must match JIM main if sharing auth)
JWT_ACCESS_SECRET=change-me-change-me-change-me
JWT_REFRESH_SECRET=change-me-change-me-change-me

# Comma-separated list of allowed browser origins
CORS_ORIGIN=http://localhost:5174

PORT=8787

# Staffing agency API keys (one per agency)
STAFFING_API_KEY_PROLOGISTIX=
STAFFING_API_KEY_STAFF_FORCE=

# Staffing punch security (warehouse Wi‑Fi allowlist)
# Comma-separated list of public egress IPs for the warehouse network.
STAFFING_ALLOWED_EGRESS_IPS=12.34.56.78,98.76.54.32
# Local dev-only bypass (still logs wifiAllowlistStatus=DEV_BYPASS on events)
STAFFING_WIFI_ALLOWLIST_DISABLED=true
# Optional dev-only bypass for specific userIds (comma-separated).
# Example: allow "Test Test" user (DTX-TT-1234) to punch off-site.
STAFFING_WIFI_ALLOWLIST_BYPASS_USER_IDS=

# Weekly report email (optional)
STAFFING_REPORT_RECIPIENTS=email1@example.com,email2@example.com
STAFFING_REPORT_CRON_SECRET=

SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

